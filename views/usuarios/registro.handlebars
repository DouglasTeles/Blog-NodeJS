{{!-- campo para exibir erros de vaidação --}}
{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div> 
{{else}}

{{/each}}



<div class="cad">
    <div class="card-header">
        <h4>Cadastro de Usuários</h4>
        <hr>
        <form action="" method="POST">
            <label for="nome" class="mt-2" >Nome</label>
            <input type="text" name="nome" class="form-control" required placeholder="Nome" >
            <label for="email" class="mt-3">E-mail</label>
            <input type="text" name="email" class="form-control" required placeholder="exemplo@exemplo.com">
            <label for="senha" class="mt-3">Senha</label>
            <input type="password" name="senha" class="form-control" required>
            <label for="senha2" class="mt-3">Confirme a senha</label>
            <input type="password" name="senha2" class="form-control" required>
            <a href=""><button type="submit" class="btn btn-success mt-4">Criar Conta</button></a>

        </form>
    </div>
</div>