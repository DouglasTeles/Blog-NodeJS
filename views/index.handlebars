<div class="bg-light p-5 rounded-lg m-3">
  <h1 class="display-4">Bem vindo ao blog de estudos!</h1>
  <p class="lead">Este blog foi desenvolvido com o intuito de aprender e praticar desenvolvimento web focado em back-end.</p>
  <hr class="my-4">
  <p>Blog desenvolvido em Node.js, Express e utilizando mongoDB como banco de dados</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>
<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4"> 
    <div class="card-body">
        
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>

        <a href="/postagem/{{slug}}"><button class="btn btn-outline-info">Saiba mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small> 
        <br>
        <small>Data de pubicação: {{data}}</small>
    </div>
</div>

{{else}}
    <h5>Nenhuma postagem</h5>
{{/each}}